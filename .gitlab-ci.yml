#
#deploy-prod:
#  stage: deploy
#  image: ubuntu:latest
#
#  script:
#    - apt-get update -y && apt-get install sshpass -y
#    - sshpass -p zaq1234 ssh -t artem@cp.c025prts.one -p 55022
#    - ls
##    - git reset --hard
##    - git pull origin main
##    - cd /home
#
##    - source venv/bin/activate
##    - gunicorn --reload -b 0.0.0.0:8000 Daylesford.wsgi
#  only:
#    - main


deploy-prod:
  stage: deploy
  image: ubuntu:latest

  before_script:

    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client git -y )'
    - mkdir -p ~/.ssh
    - eval $(ssh-agent -s)
#    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
  script:
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - ssh -o StrictHostKeyChecking=no  artem@cp.c025prts.one -p 55022 "ls;"

