
deploy-prod:
  stage: deploy

  script:
#    - git reset --hard
#    - git pull origin main
#    - cd /home
    - source venv/bin/activate
    - gunicorn --reload -b 0.0.0.0:8000 Daylesford.wsgi
  only:
    - main