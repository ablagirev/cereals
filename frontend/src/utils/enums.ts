/**
 * Типы пользовательских уведомлений.
 *
 * @prop {number} SUCCESS "Успех".
 * @prop {number} WARN "Внимание".
 * @prop {number} ERROR "Ошибка".
 * @prop {number} INFO Информационное уведомление.
 */
 export enum EToastType {
    SUCCESS,
    WARN,
    ERROR,
    INFO,
}
