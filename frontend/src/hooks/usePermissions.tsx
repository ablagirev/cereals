// import { useAuthContext } from "./useAuthContext";

/**
 * Хук, позволяющий получить разрешения текущего пользователя,
 * хранимые в контексте приложения (AuthContext).
 */
export const usePermissions = (): string[] => {
  // const authContext = useAuthContext();

  // if (authContext?.authorities) {
  //     return authContext.authorities;
  // }

  return [];
};
